XpV0(
    project: "dcc",
    containers: {
        "trainer": (
            command: ["python", "main.py", "--config=configs/enhanced.yaml"],
            dockerfile: "Dockerfile",
            copy_repo: true,
            wandb: true,

            gpu: 1,
            gpu_mem: "5GB",
        ),
        "codecraftserver": (
            dockerfile: "CodeCraftServer.Dockerfile",
            tty: true,
        ),
    }
)
